name: unit-tests
on:
  push:
    branches:
    - master
jobs:
  build-docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Install Docker
        uses: docker/setup-buildx-action@v1
        id: buildx
        with:
          install: true
      -
        name: Build
        run: docker build -f Dockerfile.dev -t fantkolja/ci-cd-tutorial .
      - 
        name: Run Tests
        run: docker run --rm fantkolja/ci-cd-tutorial "npm run test"